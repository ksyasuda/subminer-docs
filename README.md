# SubMiner Docs

Standalone VitePress documentation source for SubMiner.

## Local development

```bash
bun install
bun run docs:dev
```

Build and preview:

```bash
bun run docs:build
bun run docs:preview
```
